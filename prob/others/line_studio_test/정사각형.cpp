#include <iostream>
#include <vector>
using namespace std;

vector<int> solution(vector<vector<int> > v) {
    vector<int> ans;

    for (vector<int>::size_type i = 0; i < v.size(); ++i){
        
    }
    cout << "Hello Cpp" << endl;

    return ans;
}